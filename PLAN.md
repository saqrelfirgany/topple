# 🧩 Topple — خطة اللعبة (game #2)

_لغز فيزياء 2D — الهدم بالمقلاع · Flutter + Flame + Forge2D · بواسطة Ahmed "Saqr" ElFirgany_

## الفكرة

اسحب مقلاع للخلف وسيبه يقذف مقذوف على برج/بناء من بلوكات — اهدّه أو اضرب الأهداف بأقل عدد قذفات. كل مرحلة ليها **حل** (زاوية + قوة صح) = puzzle حقيقي، والانهيار الفيزيائي = متعة بصرية وكليبات.

**الاسم المبدئي:** Topple (يتغيّر لو حابب). **الريبو المقترح:** `topple` تحت `saqrelfirgany`.

## ليه اللعبة دي للبراند

- **سُلطة تقنية:** إتقان محرّك فيزياء حقيقي (Box2D عبر Forge2D) — collision، impulse، restitution، joints لاحقاً.
- **محتوى:** كل انهيار = كليب مُرضي. زوايا: «فيزياء واقعية في Flutter بـForge2D»، «من غير محرّك خارجي».
- **قابلة للاختبار:** 2D بيتكتبله tests (منطق المراحل + شرط الفوز) — أول tests في اللعبة زي ما عملنا في الرَنر.
- **أساس رسمي:** Flame هو المحرّك اللي Flutter نفسها بتوصّي بيه لـ2D (مخاطرة منخفضة، stable channel).

## الستاك (متأكّد منه 2026-07-31)

- `flame` **^1.38.0** — المحرّك (game loop, components, input).
- `flame_forge2d` **^0.19.3** (Box2D) — الفيزياء. بيعتمد على forge2d ^0.14.2.
- Flutter stable عادي (مش زي الرَنر اللي محتاج master). `flutter create` عادي.

## المعمارية (نفس انضباط الرَنر)

- `lib/config.dart` — **بلوك تيوننج واحد**: الجاذبية، الزوم، أحجام البلوكات، قوة القذف، الخامات، الألوان. كل الأرقام هنا.
- `lib/game.dart` — `ToppleGame extends Forge2DGame with DragCallbacks`: يبني المرحلة، يدير الحالة (عدد القذفات، الأهداف الباقية)، السحب/القذف، شرط الفوز/الخسارة.
- `lib/components/` — `Ground` (static edge)، `Block` (dynamic box)، `Target` (بلوك هدف)، `Projectile` (dynamic + `bullet:true` عشان ميخترقش).
- **الرندر:** في الـMVP الأجسام بترسم أشكالها البدائية تلقائياً (زي ما بدأنا بالرَنر بأشكال بدائية) — صفر أصول. سبرايتس/ثيم لاحقاً.

## نطاق الـMVP — 3 أيام

- **اليوم 1 (السكافولد ده):** عالم Forge2D + جاذبية + أرضية + برج بلوكات بيستقر + كاميرا + **سحب-واقذف كرة** على البرج. أول تشغيل = فيزياء شغّالة ومُرضية. ✅ (متسلّم)
- **اليوم 2:** مقلاع بصري (شريط مطاطي)، أهداف محدّدة، **شرط فوز** (كل الأهداف وقعت/اتضربت)، عدّاد قذفات، إعادة المحاولة.
- **اليوم 3:** score (أقل قذفات = نجوم)، مراحل متعددة (level layouts في data)، HUD، شاشة فوز/تالي، **بيلد ويب لايف**، أول كليب، وكارت في `../hub`.

## زوايا محتوى (كل خطوة = بوست)

- «بنيت لعبة فيزياء في Flutter من غير محرّك خارجي — Forge2D بس».
- «الفرق بين impulse و velocity وإمتى تستخدم كل واحد» (زاوية تعليمية).
- «2D vs 3D في Flutter»: نفس صاحب المشروع، لعبتين، محركين — سلسلة تكمّل الرَنر.
- كليب بطيء لبرج بينهار = محتوى بصري لوحده.

## البوتستراب (خطوات Saqr المحلية)

الكود جاهز في `games/topple/` (lib + pubspec). عشان تشغّله:

```bash
cd "games/topple"
flutter create .            # يولّد قشرة المشروع (android/ios/web/…) جنب الـlib الموجود
flutter pub get             # يسحب flame + flame_forge2d
flutter run -d chrome       # أو -d macos
```

(لو `flutter create .` عمل main.dart جديد، سيبه يستبدل بس تأكّد إن `lib/main.dart` بتاعنا هو الموجود — أو انسخه تاني.)

هوية git: `git init` + ريبو جديد `topple` تحت `saqrelfirgany`، كوميت بإسمك من غير AI trailer.

## الحلقة

أنا أكتب الكود؛ إنت تشغّله وتبعتلي سكرين شوت/إيرور. 2D أسهل في الفيدباك من الـ3D. كل خطوة شغّالة → كليب → بوست.
